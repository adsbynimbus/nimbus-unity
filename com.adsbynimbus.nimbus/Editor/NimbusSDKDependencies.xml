<dependencies>

  <androidPackages>
    <repositories>
      <repository>https://repo.maven.apache.org/maven2</repository>
      <repository>https://adsbynimbus-public.s3.amazonaws.com/android/sdks</repository>
    </repositories>

  </androidPackages>

  <iosPods>
    <!--The below pod is only to trick EDM4U into building a podfile but this is then removed and replaced dynamically
      with the actual needed pods (NimbusSDK + related) -->
    <iosPod name="NimbusSDK" version="2.20.0" bitcodeEnabled="false" 
            addToAllTargets="true"/>
  </iosPods>
</dependencies>
